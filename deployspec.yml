version: 0.2

phases:
  install:
    commands:
      - npm install -g serverless
  build:
    commands:
      - mkdir deploy
      - ls
      - cp *.json deploy/
      - cp *.zip deploy
      - serverless deploy -s codepipeline --package ./deploy


